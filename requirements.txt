# Exact pins are mandatory, not advisory. The FAISS index and the embeddings are
# written by GitHub Actions and read by the app: these are two different machines,
# and a version mismatch fails at load time with an error that is hard to
# diagnose, or worse, produces silently wrong similarity scores.
streamlit==1.40.2
httpx[http2]==0.27.2
beautifulsoup4==4.12.3
lxml==5.3.0
trafilatura==1.12.2
pysbd==0.3.4
python-docx==1.1.2
openpyxl==3.1.5
bm25s==0.2.6
extruct==0.17.0
faiss-cpu==1.9.0.post1
fastembed==0.4.2
numpy==1.26.4
